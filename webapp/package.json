{
  "name": "qbit-swap-webapp",
  "version": "0.1.0",
  "description": "Wallet-agnostic browser app for BTC<->QBT atomic swaps (ephemeral keys, keyless coordinator)",
  "type": "module",
  "scripts": {
    "build": "bash build.sh",
    "test": "node test/keystore.test.mjs && node test/broadcast_fallback.test.mjs"
  },
  "dependencies": {
    "@noble/hashes": "^2.2.0",
    "@noble/secp256k1": "^3.1.0",
    "@qbit-swap/client": "file:../client"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "playwright": "^1.61.1"
  }
}
